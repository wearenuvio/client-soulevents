# Assets

Drop the client's hero artwork here as `lotus-hero.png` (portrait orientation;
white or transparent background — the page multiply-blends it into the pink
wash either way). The homepage picks it up automatically and retires the
drawn SVG stand-in.

Future client photography lands here too, named per gallery category.
